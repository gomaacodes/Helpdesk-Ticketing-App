import { Link } from "react-router-dom";

const TicketForm = () => {
    const content = (
        <section className="form">
            <h1>form</h1>
        </section>

    )
    return content
}

export default TicketForm